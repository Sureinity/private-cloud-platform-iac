# Managed by Ansible — do not edit by hand.
# Runtime environment variables for SeaweedFS (Mode 0600, restricted to {{ seaweedfs_user }})
#
# SeaweedFS daemon options can be supplied via environment variables prefixed with WEED_.
# S3 user credentials and permissions are declared in {{ seaweedfs_s3_config_file }} (-s3.config).

{% if seaweedfs_extra_env is defined and seaweedfs_extra_env %}
{% for key, val in seaweedfs_extra_env.items() %}
{{ key }}={{ val }}
{% endfor %}
{% endif %}
